trigger QuoteTrigger on Quote (before update)
{
/*
    List<quote> AcceptedQuoteList=new list<Quote>();
   for(Quote Q:trigger.new)
   {
      if(trigger.oldmap.get(Q.id).Status!='Accepted'&&trigger.newmap.get(Q.id).status=='Accepted')
      {
         AcceptedQuoteList.add(Q);
      }
   }
    UpdateOppStageHandler opp1=new UpdateOppStageHandler();
    opp1.Updateoppstage(AcceptedQuoteList);
*/
}